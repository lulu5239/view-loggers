import Image from "next/image";

export default function Home() {
  return (
    <main>
      <h3 style="display:block">You've discovered a view-logger</h3>
      You weren't supposed to click it, it is used on Discord and Guilded to know when someone sees the message with the view-logger.
      <br>If the person that sent this view-logger is a scripter, they might have logged your IP address.
    </main>
  );
}
